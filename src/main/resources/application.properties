# (Optional) Launch in dev mode
server.port=8080

# ======================================
# DataSource Configuration
# ======================================
spring.datasource.url=jdbc:mariadb://mariadb105.r103.websupport.se:3315/flourish_db
spring.datasource.username=flourish_backend
spring.datasource.password=Wa7Z@#&5|x
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
# =================================================
# JPA / Hibernate Configuration
# =================================================
# Automatically create or update schema based on entity classes
spring.jpa.hibernate.ddl-auto=update

# (Optional) Show generated SQL in console for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# (Optional) Explicitly set the MariaDB dialect
# (Hibernate chooses the dialect automatically in most cases,
# but you can specify for clarity.)
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# Vaadin dev convenience
vaadin.launch-browser=true
spring.devtools.restart.enabled=true
